"use client";
import { useEffect, useState } from "react";

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  useEffect(() => {}, []);
  return <>{children}</>;
}
